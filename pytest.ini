[pytest]
; Garante que a pasta do projeto fica no sys.path ao correr os testes, para
; que "from scripts import atualizar_dados" (em tests/test_atualizar_dados.py)
; funcione sempre — quer se corra "pytest -v" quer "python -m pytest -v".
pythonpath = .
